{
  "name": "raajje-theft-auto-1",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cap:add": "npx cap add",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:build-apk": "npm run build && npx cap sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "phaser": "^3.80.0",
    "tone": "^14.7.77"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "vite-plugin-singlefile": "^0.13.5"
  }
}
